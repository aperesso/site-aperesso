import Layout from '../components/Layout';

const Contact = () => {
    return (
        <Layout page="contact">
            This is the contact page, we'll fix this later
        </Layout>
    )
}

export default Contact;